---
title: 00_DASHBOARD_INDEX
description: A _dashboards/ család élő indexe — minden dashboard egy sorban: verzió, cím, adatforrás, pattern, launcher-státusz, design-system megfelelőség. A Curator agent tartja frissen MINDEN build / tend / retire / promote után.
generated_by: curator (tend mode — curator/index.html Panel 12 API Reference)
last_updated: 2026-05-29
dashboard_count: 21
design_system_version: 0.9.1
id: b81d7930-f854-44dd-ae83-6d36b612d698
index_schema_version: 1
---

# Dashboard Index

> **Élő index — a Curator tartja karban.** Frissül MINDEN dashboard-művelet után (`build`, `tend`, `retire`, `promote`). A gyors keresés/navigáció alapja. Friss állapotért `survey` mód regenerálja.
>
> **Szerver:** `_dashboards/_tools/dash-server.mjs` · port **4321** · `/` → launcher · SSE a `02_Areas/**/*.md`-re. Indítás/megnyitás/leállítás: Curator `serve` mód.
> **Design system:** [`_design/DESIGN_SYSTEM.md`](_design/DESIGN_SYSTEM.md) v0.9.0 — kanonikus vizuális nyelv. **DS 0.8.13 (2026-05-27):** TEND (shared layer + launcher) — Agent Quick-Nav grid migrated from fixed 2-col × 3-row (DS 0.7.9) to adaptive `repeat(auto-fill, minmax(220px, 1fr))` to accommodate the 7th AGENT_CARDS entry (Forge 🔨 Practice Steward) without an awkward empty cell on a 4-row layout. Border-seam rule updated from `nth-child(2n)` to `:last-child` (covers any rightmost-of-row scenario in auto-fill arrangements). admin-bar.js header bumped DS 0.8.8 → 0.8.13. Launcher (`index.html`) 0.8.10 → 0.8.11: CPS Practices + ExarLabs Practices leaves added as `is-tbd` (cps/practices owned by Forge, references Inference-Farm research; exar/practices owned by Forge, references Microsites patterns-emerging). CPS sub-tree 6 → 7 branches (new `.leaves-7` grid class + `.leaves-7::after` connector + responsive fallbacks). ExarLabs sub-tree 3 → 4 branches (cols-3 retained — 4th leaf wraps to second row, least disruption). **DS 0.8.12 (2026-05-26):** TEND — Calendar pill AREA·CHANNEL meta-prefix pattern added to §11 promote-candidates. New row: `.mb-cal-pill-meta/area/ch/title` CSS + `AREA_CODES`/`CHANNEL_CODES` JS maps + `_pillMeta()` helper. presto/index.html 0.15.0→0.16.0. **DS 0.8.11 (2026-05-25):** MB Detail Drawer promote-candidates documented. **DS 0.8.10 (2026-05-25):** TEND — `wirePanelAnchors()` viselkedés javítva: (1) copy formula: origin+pathname+hash → csak pathname+hash (relatív, port-független); (2) primary click CSAK másol — nincs hash-update, nincs scrollIntoView; (3) a11y: title/aria-label → "Másolás vágólapra". clipboard.js 1.1.0 → 1.1.1. 0 dashboard HTML érintett. **DS 0.8.9 (2026-05-25):** PROMOTE — `wirePanelAnchors()` extracted to `clipboard.js` (v1.1.0). Modifier-key pass-through + a11y attrs + auto-mount on DOMContentLoaded. 8 dashboards updated (team 0.2.0, aiops 0.1.9, broker 0.5.8, sage 0.5.7, curator 0.5.8, maestro 0.5.8, librarian 0.6.0, presto 0.8.2). ~240 LOC removed. **DS 0.8.8 (2026-05-25):** AGENT_CARDS avatar sync — Presto 🎯→🐰, Sage 🌿→🦉. Avatar single source of truth: `AGENT_PROFILES.md`. §10 avatar-sync rule + canonical table added. 0 dashboard HTML touched. **DS 0.8.7 (2026-05-25):** ESC fix + letter shortcuts + `.anav-kbadge`. ESC most capture-phase listener-ből zárul (nem a drawer lazy init-jétől függ). Betű-gyorsbillentyűk (L/M/C/P/S/B) popup nyitva esetén az adott agent dashboardjára navigálnak. `.anav-kbadge`: 10px mono badge a kártya name-row-jában (badge fades on hover, arrow takes focus). admin-bar.js 0.7.9 → 0.8.0. 0 dashboard HTML touched. **DS 0.8.6 (2026-05-25):** Agent Quick-Nav layout fix: 2-col × 3-row grid (`1fr 1fr`), panel width 760px, `anav-oneliner` 2-line wrap. **DS 0.8.5 (2026-05-25):** Agent Quick-Nav bug-fix: all 6 agent cards always shown; `ensureAgentNav` grid refresh + `anav-frame` max-height safety. **DS 0.8.4 (2026-05-25):** craft polish — avatar-slot, arrow affordance, open animation. **DS 0.8.2 (2026-05-25):** Agent Quick-Nav shortcut Cmd+Shift+A → bare `A` (modifier-free); input-guard hozzáadva (INPUT/TEXTAREA/SELECT/contenteditable ignorálás); admin-bar.js 0.7.4 → 0.7.5. 0 dashboard HTML érintett. **Engine extraction milestone (2026-05-25)** — Sprint 0+1+2a+3+4+5+6 a hibrid modellből kiemelte a stabil shared layereket. Új engine fájlok `_design/`-ban: `tokens.css` (DS §1+§1a, Sprint 2a) · `theme.js`+`clipboard.js` (Sprint 1) · `markdown-parser.js` (Sprint 3, 3 dashboard) · `dom-utils.js`+`agent-logs.js` (Sprint 5, új dashboardnak) · `_template/index.html` (100% lint-zöld scaffold + ONBOARDING.md + checklist.md, Sprint 5) · `_hooks/pre-commit` (Sprint 6, opcionális git gate) · `lint.mjs` (21 szabály, --strict, --json). Anti-drift infrastruktúra: `../CLAUDE.md` auto-loaded + `_design/ARCHITECTURE.md` normatív spec (Sprint 0). 16/16 dashboard verzió-bumppolva (audit-trail entry-vel), ~1,500 LOC eltávolítva a családból. Korábbi mérföldkövek: DS 0.8.1 Agent Quick-Nav popup (Cmd+Shift+A, admin-bar.js shared layer only — 0 dashboard HTML touched); DS 0.8.0 Panel Anchor System; DS 0.6.3 mandatory card-copy-ref; 0.6.2 sidecar schema v2; 0.6.1 agent_name field rule; 0.6.0 Admin Bar; 0.4.0 LiveUpdates SSE; 0.3.0 dark theme; 0.2.0 `.card-copy-ref`.

## Család (live, `_dashboards/`)

| Fájl | Cím | Verzió | Adatforrás | Pattern | Launcher | DS |
|---|---|---|---|---|---|---|
| [`index.html`](index.html) | Ideas Vault (launcher) | 0.8.14 | — (Areas: statikus fa-navigátor; Agents: live from `/00_Prompts/BDOS/00_AGENTS_INDEX.md` 8s poll) | launcher + agent-graph | — (IS the launcher) | ✅ DS 0.3.0 — dark theme toggle + shared `dash-theme` key; area-brand tokens dark-mapped; `.card-copy-ref` on all leaf articles; v0.7.0: top-level tab switcher (#areas / #agents), D3.js force-directed agent graph, detail panel, coming-soon modal; v0.8.1: 7 agent cards removed from standalone-row (now in Agents tab graph); grid → repeat(auto-fill,minmax(260px,1fr)); v0.8.10: CPS Account Management leaf (`.leaves-6`); v0.8.11: CPS Practices + ExarLabs Practices leaves added (cps 6→7 branches, `.leaves-7`; exar 3→4 branches, cols-3 wraps); v0.8.14: graph node avatars swapped from emoji text to real Pixar portrait img, buildAvatarMap extended to all 7 agents |
| [`sales.html`](sales.html) | CPS Sales Pipeline | 0.8.1 | `/02_Areas/Sonrisa/CPS/TODAY.md` + `/02_Areas/Sonrisa/CPS/Sales/Pipeline.md` + `/02_Areas/Sonrisa/CPS/Sales/Sales Enablement/leads.md` + per-lead `NOTES.md` | per-record | ✅ live | ✅ DS 0.3.0 — dark theme toggle; pipeline-stage tokens dark-mapped; industry tag dark overrides; `.card-copy-ref` on all lead cards |
| [`account-management.html`](account-management.html) | CPS Account Management | 0.1.3 | `/02_Areas/Sonrisa/CPS/Accounts/Active/PROJECTS.md` + `/02_Areas/Sonrisa/CPS/AM_TODAY.md` + per-project `Accounts/Active/<Name>/NOTES.md` (lazy, drawer-open; falls back to `Accounts/Leads/<Name>/NOTES.md` for transitional Backlog cards) | per-record (project kanban + today panel) | ✅ live | ✅ DS 0.7.3 — built from `_design/_template/` (Sprint 5 workflow). All shared engine: tokens.css + theme.js + clipboard.js + markdown-parser.js + dom-utils.js + admin-bar.js + live-updates.js. 10-stage AM kanban (Backlog → Initial meeting → Define project need → RFP/RFI → Won → Contracted → Delivery → Renewal → Closed → Lost), summary strip (per-stage counts + total), Today panel (AM_TODAY.md current-day section), right-side drawer with lazy NOTES.md fetch + inline markdown renderer (handles Quick Info table + free-form H2 sections; no YAML frontmatter required, since Active NOTES schema differs from Lead NOTES). Card semantics: 1 card = 1 engagement, not 1 account (MVMI canonical multi-card example). Domain-specific stage tokens (`--am-backlog` ... `--am-lost`) defined locally in `:root` + dark overrides. Lint: 100% green at build. |
| [`partnerships.html`](partnerships.html) | CPS Partnerships | 0.2.10 | `/02_Areas/Sonrisa/CPS/Partnership/partners.md` | single-file | ✅ live | ✅ DS 0.3.0 — dark theme toggle + canonical dark tokens; `.card-copy-ref` on vendor cards |
| [`navigator.html`](navigator.html) | Navigátor Podcast | 0.2.2 | `/02_Areas/Navigátor Podcast/EPISODES.md` + `/02_Areas/Navigátor Podcast/TODO.md` + per-episode synthesis `.md` | per-record + single-file hybrid | ✅ live | ✅ DS 0.3.0 — bespoke dark-mode replaced with canonical implementation; shared `dash-theme` key (was `nav-dash-theme`); `--info`/`--info-tint`, `--shadow-3` preserved as navigator extras; v0.2.0: default sort desc (newest first), planned episode support (status:planned rows visible with idle badge + bg-tint + italic, no drawer on click, "Előkészítés alatt" placeholder) |
| [`aiops.html`](aiops.html) | CPS AI Ops | 0.2.1 | `/02_Areas/Sonrisa/CPS/Strategy/AI%20Ops/aiops.md` | single-file | ✅ live | ✅ DS 0.3.0 — dark theme toggle; `--prod`/`--prod-tint` dark-mapped; `.card-copy-ref` on pillar cards |
| [`team.html`](team.html) | CPS Team | 0.2.2 | `/02_Areas/Sonrisa/CPS/Team/team.md` | single-file | ✅ live | ✅ DS 0.3.0 — dark theme toggle; `--info`/`--info-tint` dark-mapped; `.card-copy-ref` on unit + person cards |
| [`plugins.html`](plugins.html) | Claude Code Plugins — Vault Fit | 0.1.9 | hardcoded (live-fetch wiring deferred) | static / hardcoded | ✅ live | ✅ DS 0.3.0 — dark theme toggle + canonical dark tokens; `.card-copy-ref` on all plugin cards; outlier status cleared |
| [`agents.html`](agents.html) | Agent Team — Cheat Sheet | 0.3.1 | `/00_Prompts/BDOS/00_AGENTS_INDEX.md` (primary, 8s poll) + `/.claude/commands/<cmd>.md` (lazy, per-drawer-open) | cheat-sheet / card-grid + clickable detail drawer | ✅ live | ✅ DS 0.3.0 — dark theme toggle + canonical dark tokens; `.card-copy-ref` on all agent cards; new: clickable agent cards open right-side drawer with full detail + per-slash-command descriptions + examples |
| [`maestro/index.html`](maestro/index.html) | Maestro — BDOS Observatory | 0.6.3 | `/00_Prompts/BDOS/00_AGENTS_INDEX.md` + `/00_Prompts/BDOS/agents/*/logs/operational/*.md` + `.../logs/learning/*.md` + `.../logs/version/*.md` (Phase 2.B pending) + `/02_Areas/Personal Growth/Ideas/_journal/YYYY-MM.md` (Sage legacy alias) | multi-source log aggregation (YAML block parser — bdos.*.log.v1 schemas) | ✅ live | ✅ DS 0.3.0 — dark theme toggle + canonical dark tokens; `.card-copy-ref` on agent + learning cards; graceful Phase-2.B empty-state on all log-driven panels; Phase 2.C token-intel placeholder |
| [`presto/index.html`](presto/index.html) | Presto — Marketing OS | 0.18.7 | `/_dashboards/_design/marketing_board.json` (kanban+calendar sidecar, scan_marketing_board.py) + `/00_Prompts/BDOS/agents/presto/_examples/_listing.md` → per-publication `.md` + channel-dna + audience-learnings + `/_dashboards/_design/agent_logs.json` (sidecar, presto-scoped) | multi-source: marketing_board sidecar + publication frontmatter + channel-dna + audience-learning frontmatter + sidecar logs | ✅ live | ✅ DS 0.8.12 — v0.17.0: panels publication-timeline + approval-queue + campaigns removed; mb-next-action info-bar removed (user-request: zsúfolt UI cleanup). v0.16.0: Calendar pill AREA·CHANNEL meta-prefix (NP·YT, DH·FB etc.). v0.15.0: Calendar pill 3-tier redesign. v0.14.0: Calendar pill keyboard accessibility. v0.13.3: Calendar pill 3-tier visual state. |
| [`librarian/index.html`](librarian/index.html) | Librarian — Knowledge Manager | 0.8.0 | `/00_Prompts/BDOS/AGENT_PROFILES.md` (capabilities, 8s poll) + `/_dashboards/_design/vault_stats.json` (total-files card) + `/_dashboards/_design/agent_logs.json` (logs panel) + `/api/search` (live search) | AGENT_PROFILES live + vault_stats sidecar + FTS5 search API | ✅ live | ✅ DS 0.3.0 — dark theme toggle + canonical dark tokens; Lora serif headings (library tone); v0.8.0: destructive tend, 3 header cards and 8 panels removed; surviving panels: 1 (total-files only), 10 (capabilities), 11 (logs), 12 (pipeline diagram), 13 (live search) |
| [`curator/index.html`](curator/index.html) | Curator — Dashboard Family | 0.5.13 | `/_dashboards/00_DASHBOARD_INDEX.md` + `/_dashboards/_design/DESIGN_SYSTEM.md` + `/00_Prompts/BDOS/agents/curator.md` | multi-file: dashboard family table parser + audit log parser + DS version parser | ✅ live | ✅ DS 0.3.0 — dark theme toggle + canonical dark tokens; self-referential ("I am the dashboard about dashboards"); graceful Phase-2.B empty-state on mode frequency + audit backlog; promote history hardcoded from DS changelog |
| [`system.html`](system.html) | BDOS System Architecture | 0.2.0 | `http://localhost:4321/health` (components map) + `/api/db/schema` + `/api/cli/run` + `/_design/system-docs/{nodes,edges}/*.md` | infrastructure / live architecture map | ✅ live (AdminBar "System" → ide) | ✅ DS — **0.2.0 (2026-05-30) clickable-graph rewrite**: CSS Grid spatial layout + SVG bezier edge overlay (27 nodes, 29 edges); both nodes AND edges clickable, opening a right-side drawer (~520px) that fetches per-node/per-edge markdown from `_design/system-docs/` and renders inline. Live /health status dot on every node, hover-highlight of connected neighbourhood, preserved CLI console + DB schema modals as drawer-resident secondary actions. 27 node docs + 10 edge docs seeded. |
| [`scheduler/index.html`](scheduler/index.html) | BDOS System Status (legacy) | 0.1.9 | `/_dashboards/_design/agent_logs.json` (sidecar, schema v2 — events + scheduled_jobs) + `http://localhost:4322/health` + `http://localhost:4321/` HEAD | infrastructure / observability | ✅ live | ✅ DS 0.6.2 — three tabs: Health (5 pills) / Jobs (reads scheduled_jobs from sidecar schema v2; fallback to event-tag logic) / Logcat (filterable 5s live stream). Doubles as ops-header drawer target. |
| [`personal-growth/index.html`](personal-growth/index.html) | Personal Growth — Weight &amp; Habits | 0.4.2 | `/05_DailyNotes/YYYY/Mon/YYYY-MM-DD.md` (Weight:: inline field, ~400 days) + `/02_Areas/Personal Growth/Habits/#*.md` (7 files, YAML `entries:` list) | multi-file: daily-note fetch + habit frontmatter | ✅ live | ✅ DS 0.6.0 — dark theme, admin-bar, LiveUpdates; Chart.js line+bar charts (Obsidian rgba colors, range toggle); weekly matrix; Habits: Obsidian-style streak pills + dot for isolated days + streak length number at pill end, range toggle (30/90/6m/1y/All, default 90), horizontal scroll with drag-to-scroll (mouse-drag pan, grab cursor), auto-scroll to latest day on render, sticky habit-name column, date header (bold Mondays, weekend cols tinted), colgroup+table-layout:fixed for pixel-perfect header/body alignment, single outer card-copy-ref (habits/all) |
| [`vezetok-imaja.html`](vezetok-imaja.html) | Vezetők Imája — Archívum | 0.1.1 | `/02_Areas/Szervezet Fejlesztés/Vezetők Imája/alkalmak.md` (master table parser, 48 rows) + `/02_Areas/Szervezet Fejlesztés/Vezetők Imája/Vezetők Imája.md` (significance frontmatter) | single-file (markdown table parser) | ✅ live | ✅ DS 0.7.3 — built from `_design/_template/` (Sprint 5 workflow first real use), 100% lint-zöld at build, all shared engine: tokens.css + theme.js + clipboard.js + markdown-parser.js + dom-utils.js + admin-bar.js. Vision block (significance list), 4 stat cards (total/years/top-speakers/missing), filter bar (search + year + speaker + missing-only chip), occasion cards (reverse-chronological, #num + date + speaker + theme + scripture + host + Drive folder/doc links). Unknown speaker/theme rendered with italic placeholder. `.card.unknown` dashed border for incomplete rows. |
| [`presences.html`](presences.html) | Presence Graph | 0.2.3 | `/02_Areas/*/presence.md` (schema: presto.org-presence.v1, 8 files) | multi-file: YAML channels array parser + per-org frontmatter | ✅ live | ✅ DS 0.7.3 — built with all shared engines (tokens.css, theme.js, clipboard.js, markdown-parser.js, dom-utils.js, admin-bar.js). Summary strip (4 stat cards), 8-org × 7-platform matrix with status dots, clickable inline channel detail panels (progressive disclosure), recovery chain cards. `.card-copy-ref` on org rows + detail panels + recovery cards. Cross-linked from Presto Channels panel. |
| [`broker/index.html`](broker/index.html) | Broker — Sales Observatory | 0.5.9 | `/_dashboards/00_SALES_INDEX.md` (not yet created) + `/00_Prompts/BDOS/agents/broker.md` + `/02_Areas/*/Sales/Cohorts/*/COHORT.md` + `.../sales-learnings/active\|proposals\|retired/*.md` + `.../reflections/*.md` | multi-source: pipeline stage parser + COHORT.md frontmatter + sales-learning frontmatter | ✅ live | ✅ DS 0.3.0 — dark theme toggle + canonical dark tokens; graceful Phase-2.B empty-state on all panels; pipeline stage tokens (6 stages); area-identity tokens (family from Presto); Presto sister cross-link; Phase 2.C token-intel placeholder |
| [`forge/index.html`](forge/index.html) | Forge — Practice Steward | 0.2.3 | per-practice `NOTES.md` frontmatter at-a-glance fields (`strategic_directions`, `next_step`, `deadline`, `blocker`, `top_todos`, `counts`, `unit_display`, `unit_short`, `maturity_stage`) from `02_Areas/Sonrisa/CPS/Practices/Inference-Farm/NOTES.md` + `02_Areas/ExarLabs/Practices/Microsites/NOTES.md` (hardcoded list, future v0.3 may discover from per-unit `00_INDEX.md`) | per-record: practice frontmatter parser + unit grouping | ✅ live | ✅ DS 0.8.14 — major rewrite from v0.1 intro page to data-driven practice observatory. Reads NOTES frontmatter from each practice, groups by `unit_display`, renders launcher-style unit containers with practice cards inside. Per practice card shows: stage badge (forming/patterns-emerging/maturing/service-ready/stable/retired with semantic color tokens), owner italic, strategic direction bullets, next-step highlight block, deadline chip (overdue/today/soon/planned/none with semantic chip colors + day-count suffix), blocker chip (red, only shown if present), top TODOs (max 3, checkbox-styled), counts row (open questions / engagements / active learnings), Open NOTES link. Shared engine FULL: tokens.css + theme.js + clipboard.js + markdown-parser.js + dom-utils.js + admin-bar.js + live-updates.js (SSE 8s sync). Forge-specific tokens (`--forge`, `--forge-deep`, `--forge-tint`) + stage tokens + deadline tokens locally in `:root` (light + dark variants). Card copy-ref (DS §4a) on every unit container, practice card, about card. "How to use me" workflow + 9-mode v0.2 preview moved to collapsible `<details class="about">` footer (de-emphasized but accessible). Lint: 100% green (regular + --strict). |
| [`alfred/index.html`](alfred/index.html) | Alfred — Daily Cockpit | 0.7.0 | `/02_Areas/Personal Growth/Alfred/state/triage_queue.md` (triage heartbeat) + `/02_Areas/Personal Growth/Alfred/tasks/00_TASKS.md` + `tasks/<YYYY-MM-DD>_<slug>.md` (dossiers, alfred.task.v1) + `state/last_run.md` + `todos/<scope>.md` + `inbox.md` + `priorities.md` | multi-file: triage heartbeat frontmatter + dossier queue index parser + dossier frontmatter + todos checkbox parser + body section parser + category-icon system | ✅ live | ✅ DS 0.9.1 — 0.7.0 tend (2026-06-07): Category-icon + priority-color system replaces read-only checkboxes. Glyph = scope (personal/family/cps/navigator/exarlabs/deak/ignis + default), color = priority (low grey, med ink, high warn), done = ok check. Inline SVG (no webfont). .cat-icon + .cat-legend + catIcon() helper. Promote candidate. Designed via /impeccable. 0.6.0 tend: Removed alfred-tasks.js sidebar (redundant on own page). 0.5.0 tend: Daily Cockpit restructure for Alfred v0.4 Triage Engine. |
**Jelmagyarázat — DS (design-system megfelelőség):** ✅ a kanonikus token-rendszert használja · ⚠️ funkcionális extra (intentionally preserved, pending promote) · ❌ szabálysértés.

## Gyors megnyitás (serve mód)

```
http://localhost:4321/                              # launcher
http://localhost:4321/_dashboards/sales.html
http://localhost:4321/_dashboards/account-management.html
http://localhost:4321/_dashboards/partnerships.html
http://localhost:4321/_dashboards/navigator.html
http://localhost:4321/_dashboards/aiops.html
http://localhost:4321/_dashboards/team.html
http://localhost:4321/_dashboards/plugins.html
http://localhost:4321/_dashboards/agents.html
http://localhost:4321/_dashboards/maestro/index.html
http://localhost:4321/_dashboards/presto/index.html
http://localhost:4321/_dashboards/librarian/index.html
http://localhost:4321/_dashboards/curator/index.html
http://localhost:4321/_dashboards/broker/index.html
http://localhost:4321/_dashboards/forge/index.html
http://localhost:4321/_dashboards/alfred/index.html
http://localhost:4321/_dashboards/presences.html
http://localhost:4321/_dashboards/scheduler/index.html
http://localhost:4321/_dashboards/personal-growth/index.html
http://localhost:4322/health                            # events_server health
```

## Pending promote — funkcionális extrák

A következő tokenek / blokkok szándékosan megmaradtak, és egy jövőbeli `promote` mód fogja eldönteni, hogy bekerülnek-e a DS-be vagy eltávolítják őket:

| Token / blokk | Hol él | Leírás |
|---|---|---|
| `--info` / `--info-tint` | navigator.html, team.html | Kék info-szín; eltérő értékek — unifikáció vagy DS-promote szükséges |
| `--shadow-3` | navigator.html | Mélység árnyék drawer-hez |
| `--prod` / `--prod-tint` | aiops.html | Exact alias of `--accent-deep`/`--accent-tint` — cleanup vagy promote |
| `--t-slow` | sales.html | Lassabb animáció-token |
| 16 pipeline-stage tokens | sales.html | `--hot`, `--warm`, `--cold`, `--contact`, `--discovery`, `--proposal`, `--won`, `--lost` + tint variánsaik |
| 13 area-brand tokens | index.html | `--branch`, `--sonrisa`, `--exar`, `--media`, `--vez`, `--ignis`, `--nav` + tint variánsaik |

*(Promoted 2026-05-22: dark-mode `:root[data-theme="dark"]` block — now canonical DS 0.3.0, rolled out family-wide.)*

## Legacy (Areas-ban, nem migrált — jövőbeli `migrate` mód)

Valódi dashboard-ok (migrálhatók):

| Fájl | Area |
|---|---|
| `02_Areas/Média Műhely/dashboard.html` | Média Műhely |
| `02_Areas/ExarLabs/dashboard.html` | ExarLabs |
| `02_Areas/ExarLabs/Clients/dashboard.html` | ExarLabs Clients |
| `02_Areas/ExarLabs/marketing/dashboard.html` | ExarLabs Marketing |
| `02_Areas/Mikado/dashboard.html` | Mikado |
| `02_Areas/Ignis Academy/dashboard.html` | Ignis Academy |
| `02_Areas/Sonrisa/Vision Corner/dashboard.html` | Sonrisa Vision Corner |
| `02_Areas/Personal Growth/Movies/dashboard.html` | Personal Growth / Movies |
| `02_Areas/Deák Húsüzlet/BIN/dashboard.html` | Deák Húsüzlet BIN |
| `02_Areas/Sonrisa/CPS/Strategy/dashboard.html` | CPS Strategy |
| `01_Projects/Gergely István/Dashboard_2025.html` | Gergely István |
| `04_Archive/Ignis/AI Kurzus/dashboard.html` | Ignis AI Kurzus (archive) |

Nem-dashboard HTML (microsites, design assets — nem migrálhatók, nem is kell):

`02_Areas/Deák Húsüzlet/Marketing/szorolap_a5.html` · `02_Areas/Sonrisa/CPS/Marketing/CPS.html` · `02_Areas/Sonrisa/CPS/Marketing/selvio/*.html` · `02_Areas/Ignis/Ignis Academy/2. szint/Pozicionalas/04_one-pager_v0.1.html` · `02_Areas/ExarLabs/design/fb-cover.html` · `02_Areas/ExarLabs/design/fb-post-microsite.html` · `02_Areas/ExarLabs/brand/fb-cover.html`

## Karbantartási napló

| Dátum | Művelet | Dashboard | Megjegyzés |
|---|---|---|---|
| 2026-06-07 | tend | alfred/index.html | 0.5.0 → 0.6.0. Removed Alfred Tasks Sidebar (aside#alfred-tasks-sidebar mount + alfred-tasks.js script include) from Alfred's own page. Rationale: the sidebar is designed to surface Alfred tasks ON other agents' dashboards; on Alfred's own cockpit page the tasks are already the primary content, making the sidebar redundant. Added margin:0 auto to .app-main for clean centered layout without the sidebar's flex context. Lint: PASS 0 errors (1 pre-existing POST-fetch warning, intentional capture endpoint). |
| 2026-05-30 | tend (shared layer) | _design/admin-bar.js (all dashboards) | admin-bar.js 0.8.22 → 0.8.23. New global 'S'/'s' keyboard shortcut: navigates to the system screen (/_dashboards/system.html), mirroring the "System" admin-bar button, analogous to 'A'/'a' opening the Agent Quick-Nav. Exception: on the system screen itself (pathname ends /system.html) it is a no-op, nothing comes up. Same input guard as 'A' (ignored in INPUT/TEXTAREA/SELECT/contenteditable and with any modifier key). SYSTEM_URL const extracted, System button reuses it. Shared-layer change: rolls out to every dashboard, no per-dashboard HTML bump. Verified in browser: librarian 's' navigates to system.html, system.html 's' no-op, 'a' still opens agent-nav. |
| 2026-05-30 | tend | librarian/index.html | 0.8.0 → 0.8.1. Live Search panel (panel-13-live-search) moved to the top: now the first content block directly after the masthead, before the Panel 1 header strip (search-first layout, user request). Markup move only, no JS/CSS change. Lint PASS (pre-existing hex warnings only). |
| 2026-05-29 | tend (destructive removal) | librarian/index.html | 0.7.0→0.8.0. Removed 3 header cards (tier2-count, version, freshness) and 8 panels (2 tier1-index, 3 tier2-units, 4 tier2-candidates, 5 knowledge-map, 6 open-questions, 7 vault-gaps, 8 recent-activity, 9 ops-reference). Dead JS removed: parseTier2, getFreshness, renderTier2, parseIndexForPARA, renderTier1, parseKnowledgeMap, renderKnowledgeMap, parseOpenQuestions, renderOpenQuestions, parseGaps, renderGaps, parseYamlFrontmatter, parseMarkdownSections, changelog modal functions and event listeners. Dead CSS removed: tier2-table, para-grid, domain-list, qg-list, candidates-grid, mode-list, changelog modal, phase-banner. pollVault() now fetches only AGENT_PROFILES_FILE. widget-strip grid shrunk to single-card. Surviving panels: 1 (total-files), 10, 11, 12, 13. Lint: PASS 0 errors (3 pre-existing hex warnings). |
| 2026-05-29 | promote (shared layer) | _design/admin-bar.js + _design/DESIGN_SYSTEM.md | admin-bar.js 0.8.21→0.8.22. Global ESC-back navigation: when no overlay is open and focus is not in a text field, ESC navigates back (history.back() or launcher fallback). Overlay-close wins (popup then drawer). Input guard: ESC inside INPUT/TEXTAREA/SELECT/contenteditable does native browser action. Zero dashboard HTML touched. DS §10 keyboard shortcuts table updated. DS 0.9.0→0.9.1. |
| 2026-05-29 | tend | _design/admin-bar.js | 0.8.20→0.8.21. checkScheduler() rewired: liveness now derived from scheduled_jobs[].last_run_at (most recent among enabled jobs) instead of scanning events[] for scheduler-tagged rows. Survives the planned DB cleanup of ~3285 scheduler/marketing-board noise rows. Graceful fallback: missing/empty scheduled_jobs renders idle/no jobs instead of throwing. SCHEDULER_STALE_MS threshold and ok/warn/idle/unknown pill states preserved. Lint (maestro): PASS 0 errors. |
| 2026-05-29 | tend | librarian/index.html + dash-server.mjs | 0.6.4→0.7.0. Phase 2 UI build: (1) vault_stats.json wired — Total Files card shows indexed/total/pct + gauge bar; Tier-2 Units Active and Index Freshness wired to tier2_active_count/fresh_count (were hardcoded 11/uncalculated). (2) Librarian Version pill made clickable → changelog modal fetching version-log YAML blocks, rendered as timeline; stale v0.6 replaced with live v0.8.3 from frontmatter. (3) (i) info icons on all 13 panels (reusable .panel-info CSS tooltip). (4) Panel 12: animated SVG retrieval pipeline (file→parse→FTS5→BM25→results), pulse animation, hover tooltips with BM25 weight details. (5) Panel 13: live search input debounced 300ms → /api/search, results with area chips + path links; typing lights up pipeline stages progressively. (6) dash-server.mjs: /api/search now records telemetry to agent_logs.json sidecar (agent_name=librarian, mode=retrieve, query_duration_ms, result_count, basis=FTS5 BM25); verified: curl cognition → 1 telemetry event in sidecar, visible in Logs panel Q ms column. |
| 2026-05-29 | tend | alfred/index.html | 0.4.1→0.4.2. Panel 08 Gyors capture: submit handler updated to POST /api/alfred/process (Haiku 4.5 immediate parse). Helper text updated: explains AI parse with inbox fallback. Success chip: green "Task felveve: scope · due" on mode:process, amber "AI nem elerheto, inboxba mentve." on mode:capture-fallback, red on HTTP error. .capture-status.warn CSS variant added. Button label updated ("Feldolgoz és ment"). Cmd/Ctrl+Enter shortcut and disabled-during-submit preserved. |
| 2026-05-29 | tend | curator/index.html | 0.5.12→0.5.13. Panel 12 Server API Reference: new card POST /api/alfred/process added (RW badge). Haiku 4.5 task parse, writes to todos/<scope>.md ## Active, fallback to inbox.md. data-card-id="api/alfred-process" + .card-copy-ref + wireCopyRef() triple (DS §4a). |
| 2026-05-29 | fix | 00_DASHBOARD_INDEX.md + curator/index.html | Two fixes. (1) Index data: a stray blank line between the forge row and the alfred row broke the family table; the contiguous-row parser stopped at forge and silently dropped alfred. Blank line removed, alfred row now joins the table (family grid 19→20 cards, alfred card+avatar now visible). Curator row version synced 0.5.11→0.5.12. (2) curator 0.5.11→0.5.12: Panel-2 family-grid avatar chips now render real Pixar portraits (/00_Prompts/BDOS/agents/<slug>/avatar.png) instead of emoji, enlarged 32px→52px, emoji kept as 404 fallback; AGENT_FILE_TO_NAME + parseAllProfiles updated to 7 agents (Alfred, Forge added, stale Sage removed). Verified in browser: 7 chips, all images load (naturalWidth 1254). |
| 2026-05-29 | tend (family-wide) | 14 dashboards | version-pill normalized family-wide to maestro canonical spec: font-size 10.5px, padding 2px 8px, font-weight 500, border-radius 999px, color ink-3, bg-elev (was 10px/1px 7px/no weight on most; ink-4/bg-sunken/radius-s on presto, personal-growth, scheduler). Markup: mono class added where missing. Librarian: masthead eyebrow chip restored (was removed in 0.5.8 as "redundant"; user wants consistency). Panel-1 libVersion widget kept. All 14 files lint-clean (PASS, 0 errors). Bumps: alfred 0.4.0→0.4.1, forge 0.2.2→0.2.3, presto 0.18.6→0.18.7, personal-growth 0.4.1→0.4.2, agents 0.3.0→0.3.1, aiops 0.2.0→0.2.1, partnerships 0.2.9→0.2.10, account-management 0.1.2→0.1.3, navigator 0.2.1→0.2.2, presences 0.2.2→0.2.3, team 0.2.1→0.2.2, sales 0.8.0→0.8.1, scheduler 0.1.8→0.1.9, librarian 0.6.2→0.6.3. broker/curator/maestro already canonical (no change). |
| 2026-05-29 | tend | maestro/index.html | 0.6.2→0.6.3. panel-7-reflection moved to directly after panel-2-agent-overview (user request: reflection panel under status/team panel). Panel id and #panel-7-reflection anchor preserved. New order: panel-5, panel-2, panel-7, panel-3, panel-4, panel-6, panel-8. |
| 2026-05-29 | tend | maestro/index.html | 0.6.0→0.6.1. panel-5-org-intelligence (Organizational Intelligence) moved to first position in the Observatory tab, directly after the masthead/top-cards block, before panel-2-agent-overview. Panel id and #panel-5-org-intelligence anchor preserved unchanged. No separate nav/jump menu to update (panels use per-panel #N self-anchors only). Comment at line 69 left as historical record. |
| 2026-05-29 | tend | curator/index.html | 0.5.10→0.5.11. Panel 12 "Server API Reference" added: static card-grid documenting all 5 dash-server.mjs routes (/ redirect, /* static, /__events SSE, /api/search, /_signals/approval, /api/alfred/capture). Per-card: method badge, R/W badge, path, purpose, request/response shape, security notes. data-card-id + .card-copy-ref + wireCopyRef() triple on all 6 endpoint cards (DS §4a). New .api-card/.api-method/.api-rw CSS added. |
| 2026-05-29 | tend | alfred/index.html + todos/personal.md + todos/family.md + dash-server.mjs | 0.3.1→0.4.0. (1) Title: "Alfréd a személyi asszisztens"; .accent → var(--accent) terracotta (canonical family color). (2) Placeholder Task A/B/C removed from todos. (3) Panel 08 Gyors capture added: textarea + POST /api/alfred/capture (new dash-server endpoint) appends to inbox.md with ISO timestamp; Cmd+Ctrl+Enter shortcut; capture-only semantics (sync mode processes later). |
| 2026-05-28 | audit+tend | alfred/index.html | DS 0.9.0 compliance pass. Panel Anchor System (DS §9) added: scroll-margin-top, .panel-anchor CSS, id+class+badge on panels 1-7. Stale Sage content removed from about footer. 0.2.1→0.3.0 (structural). |
| 2026-05-28 | retire (delete) | sage/index.html | Sage agent deprecated — merged into Alfred v0.3 (2026-05-28). sage/ directory deleted. index.html 0.8.12→0.8.13 (AGENT_DESCRIPTIONS/AGENT_DASHBOARD_URLS/STATIC_EDGES/buildAvatarMap/modal sage refs removed). admin-bar.js DS 0.8.15→0.8.16 (Sage AGENT_CARDS entry removed). dashboard_count 22→21. |
| 2026-05-22 | bootstrap | (mind) | Curator v0.2 — index létrehozva, 7 family-tag felmérve. |
| 2026-05-22 | survey (regenerate) | (mind) | Teljes újraszkennelés: adatforrások, verziók, home-link, launcher-reg, DS-drift pontosítva. Legacy HTML-ek felderítve (12 dashboard + 7 nem-dashboard asset). |
| 2026-05-22 | tend (align drift) | sales.html | `--shadow-2` és `--t-med` javítva DS 0.1.0-ra; version pill hozzáadva; 0.7.0 → 0.7.1. |
| 2026-05-22 | tend (align drift) | aiops.html | h1 type scale javítva DS 0.1.0-ra (clamp + letter-spacing); 0.1.0 → 0.1.1. |
| 2026-05-22 | tend (align drift) | index.html | Comment verzió szinkronizálva pill-hez; kanonikus status token csoport hozzáadva; area-brand tokenek megőrizve; plugins.html regisztrálva; 0.5.0 → 0.6.1. |
| 2026-05-22 | tend (review + note) | navigator.html | DS 0.1.0 ellen ellenőrizve; funkcionális extrák (--info, dark-mode) szándékosan megőrizve pending promote; 0.1.0 → 0.1.1. |
| 2026-05-22 | tend (review + note) | team.html | DS 0.1.0 ellen ellenőrizve; funkcionális extra (--info) szándékosan megőrizve pending promote; 0.1.0 → 0.1.1. |
| 2026-05-22 | migrate | plugins.html | Dark theme → DS 0.1.0 light tokenek; home button + version pill + audit trail hozzáadva; launcherben regisztrálva; 0.1.0 indulóverzió. Live-fetch wiring deferred. |
| 2026-05-22 | build | agents.html | Agent Team cheat sheet v0.1.0. Forrás: 00_AGENTS_INDEX.md (## Active agents). Kártya-grid: 3 agent (Librarian, Maestro, Curator). Mode chips, copyable slash-command chips, copyable példa-hívás sorok, autonómia callout, closing summary band. SSE + 8s poll. Launcherben regisztrálva (standalone row). DS 0.1.0 aligned. |
| 2026-05-22 | promote | (family-wide) | DS 0.1.0 → 0.2.0: `.card-copy-ref` + `data-card-id` konvenció bevezetve. Rollout mind a 8 dashboardra: agents 0.1.0→0.1.1, partnerships 0.2.0→0.2.1, aiops 0.1.1→0.1.2, team 0.1.1→0.1.2, plugins 0.1.0→0.1.1, navigator 0.1.1→0.1.2, index 0.6.2→0.6.3, sales 0.7.1→0.7.2. Ref formátum: `<stem>:<card-id>`. `overflow:visible` fix alkalmazva a stripe-os kártyákon. |
| 2026-05-22 | promote | (family-wide) | DS 0.2.0 → 0.3.0: canonical dark theme tokens + `.theme-toggle` + shared `dash-theme` localStorage key + FOUC-init + system-aware default. Rollout mind a 8 dashboardra: index 0.6.3→0.6.4, sales 0.7.2→0.7.3, partnerships 0.2.1→0.2.2, navigator 0.1.2→0.1.3 (bespoke impl replaced), aiops 0.1.2→0.1.3, team 0.1.2→0.1.3, plugins 0.1.1→0.1.2 (outlier status cleared), agents 0.1.1→0.1.2. Pipeline-stage + area-brand + industry tag dark variants added. `plugins.html` dark-theme outlier status cleared — now canonical family member. |
| 2026-05-24 | tend | agents.html | 0.1.2 → 0.2.0 (minor): clickable agent cards with right-side detail drawer (sales.html pattern). Card collapsed view uses Pozíció one-liner. Drawer sections: header (name+version+status+close), Pozíció, Capabilities detail, Felelősség, Autonómia callout, mode chips, per-slash-command rows (lazy-fetched from /.claude/commands/*.md — description + example, cached in JS memory). Keyboard-accessible (role=button, tabindex, Enter/Space, Esc, scrim-click). stopPropagation on .card-copy-ref preserved. Drawer non-destructive on live poll (cards only re-render when drawer is closed). Server confirmed to serve .claude/ files (HTTP 200). |
| 2026-05-24 | build | sage/index.html | 0.1.0 initial build. First per-agent dashboard in BDOS family. 9 panels: header status strip, last harvest, last curate, thoughts feed (live from individual .md files via 00_INDEX wikilinks), atomic grid, inbox (uncertain thoughts + atomic proposals), learnings meta-cognition panel with preamble token bar, journal timeline (YAML block parser), weekly curate highlights. Zero hardcoded content. Graceful never_run empty-state on every panel. DS 0.3.0 aligned. Registered in launcher (standalone/sage). index.html → 0.6.5. |
| 2026-05-24 | tend | index.html | 0.6.5 → 0.7.0 (minor: new feature). Top-level tab switcher added: Tab 1 "Areas" (existing hierarchy, intact) + Tab 2 "Agents" (D3.js v7 force-directed graph of BDOS agent family). Tab state persists in URL fragment (#areas / #agents). Agents graph reads live from /00_Prompts/BDOS/00_AGENTS_INDEX.md (8s poll + SSE). 15 static edges from canonical relationship map. Agent node cards: name, 5-word description, version badge, status dot. Sage card links to /_dashboards/sage/index.html (live). All other cards open a "coming soon" modal with Sage reference link. Right-side detail panel shows connections + open button. Empty-state graceful. Re-renders on theme toggle. Broker auto-appears when added to AGENTS_INDEX. |
| 2026-05-24 | build | presto/index.html | 0.1.0 initial build. Third per-agent dashboard. Marketing Observatory + Campaign Command Center + Reflection System. 10 panels: Phase 2 banner, campaign ops (active campaigns + 7-day schedule + approval queue), multi-project navigation (area color identity + platform chips), analytics + audience intelligence (atomic cross-links, resonance table, platform effectiveness), strategic reflection (weekly/monthly + drift indicators), content lineage (Sage→adapt→publish→analytics→signal thread), operational intelligence (Phase 2.C placeholder), audience learnings (8-type meta-cognition + proposals), discovery panel (4-filter status grid), Sage-signal flow (presto.sage-signal.v1). Schema-aware parsers: CAMPAIGN.md frontmatter, Results-*.md, presto.sage-signal.v1, audience-learning frontmatter. Area-identity tokens + learning-type tokens (local extensions). Graceful Phase-2.B empty-state on all panels. Launcher 0.7.1→0.7.2; AGENT_DASHBOARD_URLS updated (presto live). DS 0.3.0 aligned. |
| 2026-05-24 | build | maestro/index.html | 0.1.0 initial build. Second per-agent dashboard in BDOS family. Phase 2 Reflective Nervous System era (CONSTITUTION_PHASE_2.md §D). 6 panels: Phase 2 banner (rollout state 2.A/2.B/2.C), Agent Overview (family grid + recent activity feed), Time Navigation (weekly/monthly comparison + version timeline), Token Intelligence (Phase 2.C placeholder — all fields null, schema note), Organizational Intelligence (static dependency graph + bottleneck/risk signals), Evolution Tracking + Reflection Layer ("What did the system learn?" — open/actioned learnings with type chips). Schema-aware YAML block parser (bdos.operational.log.v1, bdos.learning.log.v1, bdos.version.log.v1 per LOG_SCHEMAS.md). Graceful Phase-2.B empty-state on every log-driven panel. Sage legacy journal alias noted. Zero hardcoded operational state. DS 0.3.0 aligned. Registered in launcher (standalone/maestro). index.html → 0.7.1. AGENT_DASHBOARD_URLS updated (maestro now live). |
| 2026-05-24 | build | librarian/index.html | 0.1.0 initial build. Knowledge Manager Observatory. 9 panels: header strip (total files indexed, tier-2 count, freshness), tier-1 PARA distribution, knowledge map domains, tier-2 scoped units table (11 units, freshness dots — fresh/stale/very-stale), tier-2 candidates (Ignis Academy 22, Média Műhely 21), open questions, gaps, recent activity (Phase 2.B empty), 6 modes reference. Lora serif headings (library/archivist tone). Tier-2 table parsed from 00_AGENTS_INDEX.md regex. DS 0.3.0 aligned. Registered in launcher (standalone/librarian). index.html → 0.7.3. AGENT_DASHBOARD_URLS updated (librarian now live). |
| 2026-05-24 | build | curator/index.html | 0.1.0 initial build. Self-referential Dashboard Family Observatory ("I am the dashboard about dashboards"). 9 panels: header strip (family count live from index, launcher version, DS version, Curator version), dashboard family grid (all 15 dashboards, self-highlighted with "← you are here"), DS overview (6 token groups, 7 laws, last promoted), mode frequency (Phase 2.B empty), 7 modes reference, audit backlog (0 findings), promote history (DS 0.1.0–0.3.0 changelog), server status (port 4321), recent builds (parsed from audit log). Dashboard family table parser + audit log section parser. DS 0.3.0 aligned. Registered in launcher (standalone/curator). AGENT_DASHBOARD_URLS updated (curator now live). |
| 2026-05-24 | tend (multi-target) | index.html + 6 per-agent dashboards | Avatar + Capabilities tend (0.7.4 / 0.2.0). All 7 dashboards: (1) emoji avatar circle in masthead — fetched live from AGENT_PROFILES.md, 8s poll, no hardcoded values. (2) Capabilities panel — 7 subsections: Tools, MCPs, APIs, Plugins, Own commands (copyable chips), Can invoke, Test access trace. parseAgentProfile() regex parser shared across all files. Launcher (0.7.3→0.7.4): avatar circles on all 6 graph nodes + capability summary chips (tools/MCPs/APIs/plugins count) in click detail panel. Per-agent dashboards (0.1.0→0.2.0): librarian, maestro, curator, sage, presto, broker. Curator special: renderDashFamily() enriched — per-card avatar chip from gAvatarMap. AGENT_PROFILES.md single source of truth — no emoji or color hardcoded in any HTML/JS. |
| 2026-05-24 | build | broker/index.html | 0.1.0 initial build. Sales Engine Observatory for BDOS Broker v0.2. 10 panels: header strip (active cohorts, total leads, win rate), cross-project pipeline (6 stage cards), today's actions queue, active cohorts grid (area-identity color tokens), conversion funnel (empty state), sales learnings (8-type lifecycle meta-cognition with active/proposed/retired counts), outreach drafts (empty), reflection panel (empty), Presto sister cross-link (distribution layer duality), operational intelligence (Phase 2.C placeholder). Pipeline stage tokens (qualification/discovery/proposal/negotiation/won/lost). Area-identity tokens (family from Presto). Graceful Phase-2.B empty-state on all panels. DS 0.3.0 aligned. Registered in launcher (standalone/broker). AGENT_DASHBOARD_URLS updated (broker now live). All 6 agent nodes in graph now link to live dashboards. |
| 2026-05-24 | promote | (family-wide, 7 dashboards) | DS 0.3.0 → 0.4.0: Live Update Pattern v0.2 (event-driven SSE replaces 8s polling). Header status indicator (`.live-status` pill: active/polling/inactive). Shared helper `_design/live-updates.js` (10KB, single source of truth). Dedicated `events_server.py` on port 4322 (clean separation from dash-server.mjs). All 7 dashboards refactored: launcher 0.7.4→0.7.5, sage/maestro/curator/presto/librarian/broker 0.2.0→0.3.0. Polling fallback preserved (auto-trigger after 3s SSE-fail). Sub-second update latency. Curator v0.4→v0.5. |
| 2026-05-24 | build (infra) | (observability stack) | Phase B — Agent Observability Stack. Created `agent_observability.db` (SQLite WAL, FTS5, 13 indices, triggers) in `cache/`. Written `agent_log.py` (writer API: `log_event()`, `AgentLogger` class, model cost table, atomic sidecar refresh). Written `agent_log_query.py` (reader API: 12 filter primitives, `agent_summary()`, `global_stats()`, FTS5 search). Seeded 34 test rows (8 scenarios, all 6 agents). Sidecar JSON: `_design/agent_logs.json` (19,826 bytes, last-500-events export). Write p50=4.8ms, p95=13.5ms. All query latencies <3ms. |
| 2026-05-24 | tend | maestro/index.html | 0.3.0 → 0.4.0. Logcat global tab added (Observatory tab preserved). 6 cross-agent summary cards. Full filter strip: agent, project, level, event_type, time range, model, tool, token threshold, duration threshold, text search, error-only, slow-op (12 chip groups). Logcat table: ts, agent, mode, project, level, event_type, message, model, tokens, cost, duration. URL hash persistence (#observatory / #logcat). Wired to `_design/agent_logs.json` sidecar (change-on-diff guard via `generated_at`). |
| 2026-05-24 | tend | librarian/index.html | 0.3.0 → 0.4.0. Logs panel added: 6 summary cards (token totals, avg duration, error count, recent events, slowest op, highest-token op). Scoped to LOGS_AGENT='librarian'. 9 seed rows visible. Wired to sidecar; LiveUpdates.subscribe() extended. |
| 2026-05-24 | tend | sage/index.html | 0.3.0 → 0.4.0. Logs panel added. Scoped to LOGS_AGENT='sage'. 4 seed rows visible. Wired to sidecar. |
| 2026-05-24 | tend | curator/index.html | 0.3.0 → 0.4.0. Logs panel added. Scoped to LOGS_AGENT='curator'. 9 seed rows visible. Wired to sidecar. |
| 2026-05-24 | tend | presto/index.html | 0.3.0 → 0.4.0. Logs panel added. Scoped to LOGS_AGENT='presto'. 4 seed rows visible. Wired to sidecar. |
| 2026-05-24 | tend | broker/index.html | 0.3.0 → 0.4.0. Logs panel added. Scoped to LOGS_AGENT='broker'. 4 seed rows visible. Wired to sidecar. |
| 2026-05-24 | promote (Phase B) | all 15 + 1 new | DS 0.4.0 → 0.5.0. BDOS Job Scheduler: `scheduler.py` + `scheduler_schema.sql` (schema v1.3 — `scheduled_jobs` + `job_runs`). `events_server.py` wired with `scheduler_loop()` daemon thread (60s scan). `ops-header.js` shared helper. `scheduler/index.html` (Health/Jobs/Logcat). Ops-header injected into all 15 existing dashboards (version bumps: index 0.7.5→0.8.0, sales 0.7.3→0.7.4, partnerships 0.2.2→0.2.3, navigator/aiops/team 0.1.3→0.1.4, agents 0.2.0→0.2.1, plugins 0.1.2→0.1.3, sage/maestro/librarian/curator/presto/broker 0.5.0→0.5.1). Sage plist unloaded + removed. Sage daily/weekly jobs seeded in `scheduled_jobs`. device_id generated at `~/.bdos/device_id`. |
| 2026-05-24 | tend (schema realign, family-wide) | maestro + 6 agent dashboards | 0.4.0 → 0.5.0 (all 7). Schema v1.2 realignment: `agent_events`→`agent_logs` table, `log_level` replaces `level`, `agent_name` replaces `agent`, `input_tokens`/`output_tokens`/`total_tokens` replace `tokens_in`/`tokens_out`, `timestamp` replaces `ts`, `task_id` replaces `session_id`, `status` replaces `outcome`. 15 event_type vocabulary (was 8). 6 log_level vocabulary (`notice`/`warning`/`critical` replace `info`/`warn`/`fatal`). Maestro logcat: filter chips, column headers, JS fully updated; `Q ms` column added for `query_duration_ms`; 10-column layout. Librarian: slowest-query callout box, `Q ms` column header + cell rendering. All 6 agent per-page logs panels updated. DB verified: `agent_logs` table, 28 columns, 34 migrated rows, FTS5 on title+message. |
| 2026-05-24 | canonical update (Phase C) | (all 6 agent canonicals + registrations) | Phase C documentation pass complete. `## Scheduling v1` section added to all 6 agent canonical files. Patch bumps: Librarian 0.8.2→0.8.3, Maestro 0.5.2→0.5.3, Curator 0.5.2→0.5.3, Sage 0.4.2→0.4.3, Presto 0.5.2→0.5.3, Broker 0.3.2→0.3.3. All 6 registration files lockstep synced. `CONSTITUTION_PHASE_6.md` written (scheduler control plane, launchd deprecation, multi-device SQLite locks, 9 job states, requires_approval semantics, scheduler tag taxonomy). `LOG_SCHEMAS.md` bumped 1.2→1.3 (§0.S scheduler tag-discriminator + §0.J DDL cross-reference). `00_AGENTS_INDEX.md` maintenance logs updated with new patch versions. Scheduler family rollout complete. |
| 2026-05-25 | promote (DS 0.5.0 → 0.6.0, family-wide, 15 fájl) | all 15 + index.html (Task-1 fix) | Admin Bar rollout: `#bdos-ops-header` → `#bdos-admin-bar`, `ops-header.js` → `admin-bar.js`. Version bumps: index 0.8.1→0.8.2, sales 0.7.4→0.7.5, partnerships 0.2.3→0.2.4, navigator/aiops/team 0.1.4→0.1.5, plugins 0.1.3→0.1.4, agents 0.2.1→0.2.2, scheduler 0.1.1→0.1.2, sage/maestro/librarian/curator/presto/broker 0.5.1→0.5.2. index.html Task-1 fix: `<div id="bdos-admin-bar">` + admin-bar.js script tag were missing — both injected. ops-header.js backward-compat alias remains active. |
| 2026-05-25 | build | personal-growth/index.html | 0.1.0 initial build. Personal Growth — Weight & Habits dashboard. Three tabs: Weight (Chart.js line chart raw+EMA-14 + range toggle 30d/90d/6m/1y/All + bar chart monthly EMA delta), Weekly Matrix (5-week window, daily weights, weekly avg, WoW delta, 8-week linreg forecast 2 weeks), Habits (7 habit × 120-day heatmap, current + longest streak). Data: daily notes Weight:: inline field (Dataview pattern, NOT frontmatter — new parser convention) + Habits/#*.md YAML entries list. Admin-bar (DS 0.6.0). index.html 0.8.2 → 0.8.3. |
| 2026-05-25 | promote (bugfix, 6 dashboards) | librarian/curator/sage/presto/broker/maestro | 0.5.2 → 0.5.3. JS filter fix: `e.agent` → `e.agent_name` in LOGS_AGENT filter (sidecar JSON canonical field). Stale Phase 2 banner removed from all 6. DS 0.6.0 → 0.6.1: §7 agent_name field name rule codified. Maestro: banner removed, JS filter chains untouched (intentional multi-source fallback). |
| 2026-05-25 | tend | index.html | 0.8.0 → 0.8.1. Removed 7 agent/BDOS cards from standalone-row (standalone/agents, /sage, /maestro, /presto, /librarian, /curator, /broker) — these are discoverable via the Agents tab graph; redundant in Areas tab. standalone-row grid changed from `repeat(3,1fr)` to `repeat(auto-fill,minmax(260px,1fr))` for responsive layout with remaining 4 cards. |
| 2026-05-25 | tend | personal-growth/index.html | 0.1.0 → 0.1.1. card-copy-ref added per DS 4a. 15 bounded cards now carry data-card-id + hover copy-ref button: stats/latest-weight, stats/trend-30d, stats/delta-30d, stats/habits-today (4 stat cards); charts/weight-line, charts/weight-delta (2 chart cards); matrix/weekly (1 matrix card); habits/all (outer habit card); habits/reggeli, habits/vitamin, habits/mozgas, habits/hidegzuhany, habits/olvasas, habits/adatok, habits/tanulas (7 habit rows, JS-rendered + wireCopyRef). CSS .card-copy-ref block added verbatim from DS 4a. position:relative ensured on .card, .stat-card, .habit-row. copyText + wireCopyRef helpers added. DASH_STEM already defined. |
| 2026-05-25 | tend | librarian/index.html | 0.5.3 → 0.5.4. Panel anchor system: id="panel-N-slug" on all 11 panel wrappers + .panel-anchor hover #N links in each header. CSS: .panel-anchor (opacity:0 → 1 on host hover, JetBrains Mono 10px, accent-tint on hover) + [id^="panel-"] scroll-margin-top:42px (clears fixed 34px admin bar). Scheme: panel-1-header-strip … panel-11-logs. New pattern — candidate for promote to family. |
| 2026-05-25 | tend | personal-growth/index.html | 0.3.1 → 0.4.0. Habits panel UX fixes: (1) alignment fix — `table-layout:fixed` + `<colgroup>` with explicit `130px` name col + `26px` day cols eliminates header/body column drift; (2) auto-scroll to latest — after each `renderHabits()` call `requestAnimationFrame` sets `scrollLeft = scrollWidth − clientWidth` so today is always visible; (3) drag-to-scroll — `mousedown/move/up/leave` handler on `.habits-scroll-wrap`, `cursor:grab/grabbing`, 1.5× multiplier, guards `.card-copy-ref` target; clipboard.js `stopPropagation` preserved. `initDragScroll()` wired once on `DOMContentLoaded`. |
| 2026-05-25 | tend | personal-growth/index.html | 0.2.1 → 0.3.0. Habits panel: Obsidian Habit Tracker visual style ported — streak pills (connected filled rects with accent color), isolated day dot, streak length number at pill end (if ≥2). Range toggle (30/90/6m/1y/All, default 90d). Horizontal scroll with sticky habit-name column. Date header row (day numbers, bold Monday, weekend cols tinted). Per-row card-copy-ref removed; only outer habits/all card retained. HABIT_DISPLAY names with emoji. computeStreaks() helper. gHabitRangeDays state. |
| 2026-05-25 | build + register | presences.html + presto/index.html + index.html | Presence Graph v0.2.0 built (cross-org matrix, clickable channel detail cards, recovery chains). Registered in launcher (standalone/presences) → index.html 0.8.7→0.8.8. Cross-linked from Presto Channels panel → presto/index.html 0.6.3→0.6.4. |
| 2026-05-25 | promote (DS 0.8.0, family-wide, 15 HTML files) | all family except personal-growth + vezetok-imaja + index | Panel Anchor System rollout. Every scrollable panel/section in multi-panel dashboards gets `id="panel-N-slug"` + `.panel-anchor` hover badge + `wirePanelAnchors()` JS (click copies URL with hash, smooth-scrolls). Tab-based and single-grid dashboards get scroll-margin-top: 42px only (no badge anchors). Version bumps: librarian 0.5.8→0.5.9 (11 panels), broker 0.5.6→0.5.7 (12 panels), curator 0.5.6→0.5.7 (11 panels), maestro 0.5.5→0.5.7 (8 panels), sage 0.5.5→0.5.6 (11 panels), presto 0.8.0→0.8.1 (11 panels), team 0.1.8→0.1.9 (4 panels), aiops 0.1.7→0.1.8 (3 panels), partnerships 0.2.7→0.2.8 (2 panels/IDs), navigator 0.1.8→0.1.9 (tab-exempt), presences 0.2.0→0.2.1 (3 panels/IDs), sales 0.7.7→0.7.8 (2 panels/IDs), agents 0.2.4→0.2.5 (1 grid ID), plugins 0.1.7→0.1.8 (1 grid ID, single-grid), scheduler 0.1.6→0.1.7 (tab-exempt). DS 0.8.0 § 9 Panel Anchor System codified. |
| 2026-05-25 | promote (DS 0.8.8 → 0.8.9, shared layer) | clipboard.js 1.0.0 → 1.1.0 + 8 dashboards | wirePanelAnchors() extracted from 8 inline dashboard definitions into /_design/clipboard.js. New behavior: modifier-key pass-through (middle/Cmd/Ctrl/Shift+click open natively); a11y attrs set at wire time (aria-label="Panel link másolása", title="Click to copy panel link"); auto-mount on DOMContentLoaded — no per-dashboard boot call. Dashboards bumped: team 0.1.9→0.2.0, aiops 0.1.8→0.1.9, broker 0.5.7→0.5.8, sage 0.5.6→0.5.7, curator 0.5.7→0.5.8, maestro 0.5.7→0.5.8, librarian 0.5.9→0.6.0, presto 0.8.1→0.8.2. ~240 LOC removed. DS §9 JS spec updated to reflect shared-layer canonical. |
| 2026-05-25 | tend (shared layer, DS 0.8.7 → 0.8.8) | admin-bar.js only — 0 dashboard HTML touched | AGENT_CARDS avatar sync. Presto emoji 🎯→🐰, Sage emoji 🌿→🦉. Single source of truth codified: `AGENT_PROFILES.md` avatar.emoji field. DS §10 updated with avatar-sync rule + canonical 6-agent table. admin-bar.js comment header + CSS block version bump to DS 0.8.8. |
| 2026-05-25 | tend (shared layer, DS 0.8.6 → 0.8.7) | admin-bar.js only — 0 dashboard HTML touched | ESC fix + letter shortcuts + shortcut badge. (1) ESC: dedicated capture-phase keydown listener at mount() — popup ESC no longer depends on drawer lazy-init. (2) Letter quick-jump: `_agentLetterMap` built dynamically from AGENT_CARDS[i].name[0] at mount; when popup open, L/M/C/P/S/B navigates to agent dashboard. First-wins collision rule + console.warn. (3) `.anav-kbadge`: 10px JetBrains Mono badge in name-row between .anav-name and .anav-arrow; var(--bg-sunken) bg, fades to 0.4 on hover. DS §10 updated with capture-phase ESC spec + letter-jump docs + badge CSS spec. admin-bar.js 0.7.9 → 0.8.0. |
| 2026-05-25 | tend (shared layer, DS 0.8.5 → 0.8.6, bug-fix) | admin-bar.js only — 0 dashboard HTML touched | Agent Quick-Nav layout fix: 2-col × 3-row grid + overflow fix. Root cause of 4-card/2×2 bug: `repeat(3, 1fr)` on 680px panel creates 3 very narrow columns (~213px each); the `anav-oneliner` with `white-space: nowrap` caused cards to overflow/clip, and the 520px viewport media query never fires on wide browser windows. Fix: (1) `grid-template-columns: 1fr 1fr` (2-col × 3-row — all 6 agents in 3 rows, more horizontal space per card); (2) panel `width: min(96vw, 760px)` (was 680px); (3) `anav-oneliner` `white-space: normal` + `-webkit-line-clamp: 2` — Maestro long text wraps instead of overflowing; (4) removed superseded `@media (max-width: 520px)` 2-col override; (5) border-right rule updated to `:nth-child(2n)` (was `:nth-child(3n)`). admin-bar.js 0.7.8 → 0.7.9. |
| 2026-05-25 | tend (shared layer, DS 0.8.4 → 0.8.5, bug-fix) | admin-bar.js only — 0 dashboard HTML touched | Agent Quick-Nav shows all 6 cards. Root cause: `ensureAgentNav()` early-returned when `#bdos-agent-nav` already existed in DOM — a stale overlay built from a prior AGENT_CARDS (4 entries) was reused. Fix: overlay is created once; on every `openAgentNav()` call the `#bdos-agent-nav-grid` innerHTML is refreshed from the live AGENT_CARDS array (cheap, no event-listener churn). Safety CSS: `anav-frame` gets `max-height: calc(100vh - 80px); overflow-y: auto` so tall content never clips on short viewports. admin-bar.js 0.7.7 → 0.7.8. |
| 2026-05-25 | tend (shared layer, DS 0.8.3 → 0.8.4) | admin-bar.js only — 0 dashboard HTML touched | Agent Quick-Nav popup craft polish. Typography ratio anav-name 13.5px/oneliner 10px (1.35, was 1.14). Spacing rhythm: header/card/footer distinct padding zones. Avatar-slot (.anav-avatar 36px radius-m square). Arrow affordance (.anav-arrow translateX, ease-out-quart). Hover: shadow-1 + bg-tint. Focus ring 3px offset. Open animation: scrim 120ms / frame scale+opacity 200ms. Footer underline on hover. DS 0.8.4, admin-bar.js 0.7.6 → 0.7.7. No dashboard HTML touched. |
| 2026-05-25 | tend (shared layer, DS 0.8.2 → 0.8.3) | admin-bar.js only — 0 dashboard HTML touched | Agent Quick-Nav popup: panel switched to light surface. `.anav-frame` and `.anav-card` now use DS page tokens (var(--bg-elev), var(--bg-tint), var(--ink-1/3), var(--line/line-soft), var(--accent)). Scrim/backdrop stays dark (rgba(20,20,19,.55) + blur — invariant). Focus rings added to close button, cards, footer link. DS §10 visual spec updated: panel = light surface, scrim = dark backdrop (invariant). admin-bar.js 0.7.5 → 0.7.6. |
| 2026-05-25 | promote (shared layer, DS 0.8.0 → 0.8.1) | admin-bar.js only — 0 dashboard HTML touched | Agent Quick-Nav popup + global keyboard shortcut. Cmd+Shift+A / Ctrl+Shift+A toggles centered modal overlay (blur backdrop, ESC + outside-click closes). 6 agent mini-cards: emoji + name + 1-liner + absolute link per agent dashboard. Footer "Open full graph →" → index.html#agents. .ab-agents-btn added to admin bar right zone. AGENT_CARDS static list in admin-bar.js. DS new §10 (keyboard shortcuts + Agent Quick-Nav pattern). admin-bar.js version bump DS 0.6.0 → DS 0.7.4 in header comment. |
| 2026-05-25 | tend | presto/index.html | 0.6.0 → 0.6.1. Phase 2: Distribution Intelligence. Signal-write approval pattern (POST `/_signals/approval` → `approval_signal.v1` files in `_inbox/approval-actions/`). 4 new panels: Comments Inbox, TODO Inbox, Insights lifecycle, Templates lifecycle (all empty-state ready, wired to `todos/`, `insights/`, `templates/` listing dirs). Approval Queue upgraded: green Approve / red Reject signal-write buttons + CLI fallback. Channel DNA cards: `extractField()` + `extractOverrides()` helpers for robust nested YAML parsing. YAML array parser fix: unquoted `[a, b, c]` now splits correctly. `dash-server.mjs` extended with POST `/_signals/approval` endpoint (schema-validated, broadcasts SSE on write). Presto canonical bumped v0.5.3→v0.6.0: 7 new modes (publish, comment-scan, comment-reply, insight, template, channel, todo), 19 modes total. 6 new slash commands (pres-publish, pres-approve, pres-channel, pres-todo, pres-template, pres-insight). Lint: 0 errors, 1 expected warning (POST fetch — promoted exception). |
| 2026-05-25 | rebuild | presto/index.html | 0.5.5 → 0.6.0. Marketing OS rebuild (Phase 1). Complete rewrite from Observatory to Marketing Cognition cockpit. New panels: Daily Briefing Strip (4 stat cards), Time Board (horizontal day-columns with publication cards sorted by scheduled_time), Approval Queue (copyable `/pres-approve pub:<project>/<campaign>/<pub-id>` commands — invariant-safe Phase 1 pattern), Channel DNA Browser (4 channels: linkedin/x-twitter/instagram/youtube with per-Area tone overrides). Preserved: Audience Learnings, Operational Log (sidecar-scoped). Removed/deferred: Phase 2 Banner, Campaign Operations, Analytics, Reflection, Lineage, Discovery, Sage-Signals (Phase 2-4). New atomic unit: PUBLICATION.md (presto.publication.v1 schema, 25+ frontmatter fields, 14 lifecycle states). Channel DNA files (presto.channel-dna.v1 schema, cross-project canonical with per-Area overrides). Architecture doc: `00_Prompts/BDOS/agents/presto/MARKETING_OS_ARCHITECTURE.md`. 5 sample publications + 4 channel DNA files created. `_listing.md` convention for dashboard file discovery. DS 0.7.3 fully aligned (tokens.css link, shared JS imports, FOUC-init, admin-bar). Lint: 0 errors. |
| 2026-05-25 | tend | librarian/index.html | 0.5.7 → 0.5.8. Masthead cleanup: eyebrow version-pill removed (redundant with Panel 1 Librarian Version widget). Submeta row 1 shortened to "Vault retrieval & indexing infrastructure". agentVersion span removed from submeta (redundant with Panel 1 widget). Panel 1 widget sub-text: "6 modes · Phase 2.B" badge added. |
| 2026-05-25 | tend | personal-growth/index.html | 0.1.1 → 0.2.0. Chart styles ported verbatim from Obsidian _views/weight-chart and _views/weight-delta (DS-independent rgba colors, gridlines, hover-all-values interaction mode, full annotation set). Line chart: Weight rgba(130,160,210,0.6) blue with fill; EMA-14 rgba(239,68,68,1) red borderWidth:3; interaction:{mode:"index",intersect:false} for hover-all; minLine/maxLine vertical line annotations (green/red with label+bg); trendMinLine horizontal dashed rgba(239,68,68,0.35) with dynamic label ("Today all-time low" or "Min trend +delta"); y/x grid rgba(100,100,100,0.1); legend bottom; title includes reading count. Bar chart: migrated from monthly-bucket to day-by-day EMA delta vs 30d ago (exact view.js logic with ±3d fuzzy match); green rgba(34,197,94,0.7) down / red rgba(239,68,68,0.7) up; zeroLine annotation rgba(100,100,100,0.8); range toggle 30d/90d/6m/1y/All added to bar card (gBarRangeDays state); legend bottom; tooltip sign+kg vs EMA 30 days ago. |
| 2026-05-26 | tend | navigator.html | 0.1.9 → 0.2.0. Default sort desc (newest EP first). Planned episode support: status:planned rows pass filteredSorted(), shown with is-planned class (bg-tint bg, 0.72 opacity, idle badge, italic guest). fSpotify dropdown: "Tervezett" option added. renderWidgets(): published vs planned split (cap: "N publikált · M tervezett"). renderEpisodes(): planned rows show "Előkészítés alatt" placeholder instead of YT icon; planned badge instead of Spotify status; no drawer on click; views shown as "—". EPISODES.md: EP43 (Gyász — Farkas Kinga, today target), EP44 (Miklós Ervin, future), EP45 (AI képzés, recorded) added with status:planned. |
| 2026-05-25 | tend (shared layer, DS 0.8.9 → 0.8.10) | clipboard.js 1.1.0 → 1.1.1 — 0 dashboard HTML touched | wirePanelAnchors() viselkedés javítva: copy formula origin+pathname+hash → pathname+href (relatív, port-független); primary click tisztán csak clipboard write (nincs hash-update, nincs scroll); title+aria-label → "Másolás vágólapra". DS §9 spec frissítve. |
| 2026-05-25 | tend (P3 Marketing Board) | presto/index.html | 0.8.2 → 0.9.0. Marketing Board panel added. 6-lane kanban (Seed/Draft/Prepared/Approval/Scheduled/Published) + Calendar view (Week/Month/Day, default Week). Tab switching. Empty-state with copyable `/pres-seed`. Next-action recommend box (most-stalled seed/draft → copyable command). Reads `/_dashboards/_design/marketing_board.json` sidecar. Sidecar generator `scan_marketing_board.py` written (example-filter, graceful skip, per-file error handling). DS promote-candidates noted: `.mb-lane` kanban-lane pattern + `.mb-cal-grid` calendar-grid pattern. Lint: 0 errors, 2 pre-existing warnings. |
| 2026-05-26 | tend | presto/index.html + events_server.py | 0.11.0 → 0.12.0. Marketing Board live refresh (SSE-driven, no F5). events_server.py: `MARKETING_BOARD_SIDECAR` const + `get_mb_mtime()` + parallel mtime check in `watcher_loop()` — broadcasts vault-update when marketing_board.json changes. presto/index.html: `loadMarketingBoard()` change-detection (generated_at/lane-count sig), drawer-safe re-render (skips kanban/cal innerHTML when drawer .open, only updates count badge silently), `.mb-live-dot` (7px var(--accent) circle, `mb-live-pulse` keyframe, 3s visibility on confirmed change). |
| 2026-05-26 | tend | presto/index.html | 0.13.3 → 0.14.0. Calendar pill keyboard accessibility: tabindex="0" + role="button" on clickable pills; onkeydown Enter/Space triggers openMBDetail() + preventDefault(); :focus-visible outline 2px var(--accent) offset 2px; title updated to "<name> (kattints a részletekhez)" when clickable. DS-compliant: no new tokens. |
| 2026-05-26 | tend | presto/index.html | 0.16.0 → 0.17.0. Panels removed: publication-timeline (panel-2), approval-queue (panel-3), campaigns (panel-4). mb-next-action info-bar removed. JS: wireCopyRef(panel-2) orphan removed; _renderMbNextAction function + 2 call-sites removed; renderApprovalQueue() + renderCampaigns() calls removed from boot. CSS: .mb-next-action + .mb-next-action-label rules removed. briefing strip + kanban + calendar + channels + all remaining panels unaffected. |
| 2026-05-26 | tend | presto/index.html | 0.15.0 → 0.16.0. Calendar pill AREA·CHANNEL meta-prefix layer. Every calendar pill now carries a monospace 2-token prefix (AREA·CHANNEL) before the title. New JS consts: `AREA_CODES` (6 entries, IGN 3-char exception) + `CHANNEL_CODES` (12 entries) + `_pillMeta(p)` helper. HTML anatomy: `.mb-cal-pill-meta` → `.mb-cal-pill-area` (opacity 0.55, title attr) + `.mb-cal-pill-ch` (opacity 1, title attr) + `.mb-cal-pill-title` (flex:1 ellipsis). CSS: `.mb-cal-pill` → inline-flex gap 5px; `.mb-cal-pill-meta` (ui-monospace 8.5px 700 font-style:normal); color inherited per tier (#fff/ink-1/ink-3). `::before` margin-right 1px (tighter gap). DS §11 promote-candidate row added. DS 0.8.11 → 0.8.12. Kanban untouched. 3-tier visual untouched. Click/keyboard/drawer untouched. Lint: green. |
| 2026-05-26 | tend | presto/index.html | 0.13.2 → 0.13.3. Calendar pill 3-tier visual state (Curator). Tier 1 Locked (scheduled/published + scheduled_* status override): opacity 1, solid bg. Tier 2 Approved (approved + approved_* status override): opacity 0.45, dashed border 1.5px. Tier 3 Plan (seed/draft/prepared/approval): opacity 0.5, repeating-linear-gradient -45deg diagonal stripe on channel-color bg. Hover: locked 0.85, approved 0.65, plan 0.7. JS: _mbCalApprovalClass() accepts full item object (status overrides stage via prefix match); _MB_CAL_APPROVED_STAGES Set added; old 2-tier .mb-cal-pill-pending replaced. Kanban, drawer, tooltips untouched. Lint: 0 errors, 2 pre-existing warnings. |
| 2026-05-26 | tend | presto/index.html | 0.13.0 → 0.13.1. Calendar approval-state opacity rule. Non-approved stages (seed/draft/prepared/approval) → .mb-cal-pill-pending (opacity 0.5, hover 0.8). Locked stages (approved/scheduled/published/approved_pending_manual_publish) → .mb-cal-pill-locked (opacity 1, hover 0.85). New helper _mbCalApprovalClass() + _MB_CAL_LOCKED_STAGES Set. Kanban, drawer, tooltips untouched. DS-compliant: opacity-only, no new token. |
| 2026-05-26 | tend | presto/index.html | 0.10.0 → 0.11.0. Navigator area-dashboard link integration (TP1+TP2). `AREA_DASHBOARDS` const maps org names to dashboard URLs (currently: `Navigátor Podcast → /_dashboards/navigator.html`). TP1: `renderChannelsCards()` injects `.area-dash-link` pill ("Area Dashboard →" + 2×2 grid SVG icon) in `channel-org-header` when org has a mapping. TP2: MB Detail Drawer Targeting section — Area row in both `_renderMBDetailSeed` and `_renderMBDetailPublication` appends `.area-dash-link-sm` (↗) when area matches map. New CSS: `.area-dash-link`, `.area-dash-link-sm` (DS-token-only, promote-candidates — Presto-specific for now). |
| 2026-05-27 | build | account-management.html | 0.1.0 initial build. Second AM-engine dashboard in CPS (sibling of sales.html). Built from `_design/_template/` (Sprint 5 workflow). 10-stage kanban (Backlog → Initial meeting → Define project need → RFP / RFI → Won → Contracted → Delivery → Renewal → Closed → Lost) parsed from `Accounts/Active/PROJECTS.md`. Today panel parsed from `AM_TODAY.md` (current day section). Right-side drawer lazy-fetches `Accounts/Active/<Name>/NOTES.md` (or `Accounts/Leads/<Name>/NOTES.md` for transitional Backlog cards), strips frontmatter, renders Active NOTES schema (Quick Info table + free-form H2 sections — no enforced YAML, unlike Lead NOTES). Summary strip: per-stage count + total (11 cards). Domain-specific stage tokens (`--am-backlog`…`--am-lost`) defined locally in `:root` + dark-theme override + prefers-color-scheme fallback. Card semantics: 1 card = 1 engagement, not 1 account (MVMI = 3 sub-engagement cards). All shared engine: tokens.css + theme.js + clipboard.js + markdown-parser.js + dom-utils.js + admin-bar.js + live-updates.js. Card copy-ref (DS §4a) on every project-card, summary-card, and the 2 panels. Lint: 100% green at build (0 error, 0 warning, 0 info). Launcher registered: `cps/account-management` leaf added to CPS sub-tree (now 6 branches, was 5); new `.leaves-6` CSS grid class added. index.html 0.8.9 → 0.8.10. |
| 2026-05-27 | tend (multi-target) | index.html + admin-bar.js | Forge agent added to AGENT_CARDS popup (7th entry, 🔨, /_dashboards/forge/index.html — dashboard TBD). CPS Practices + ExarLabs Practices leaves added to launcher (is-tbd). CPS 6→7 branches (new `.leaves-7` grid class + `.leaves-7::after` connector + responsive 980/640px fallbacks). ExarLabs 3→4 branches (cols-3 retained — 4th leaf wraps to second row, least disruption). Popup grid migrated to adaptive `repeat(auto-fill, minmax(220px, 1fr))` for 7+ agents (replaces fixed `1fr 1fr` 2-col × 3-row from DS 0.7.9). Border-seam rule `nth-child(2n)` → `:last-child` (covers any rightmost-of-row in auto-fill). admin-bar.js DS 0.8.8 → 0.8.13. Launcher 0.8.10 → 0.8.11. AGENT_PROFILES.md Forge section + cross-family row added (separate edit by main Claude). Lint: 100% green (regular + --strict). |
| 2026-05-27 | tend | admin-bar.js | "Open full graph" link removed from Agent Quick-Nav popup footer (user-reported dead button — `#agents` fragment had no listener on launcher). Removed: `fullGraphUrl` const, `<div class="anav-footer">…</div>` HTML block, CSS rules for `.anav-footer` / `.anav-full-link` / hover / focus-visible. Launcher Agents tab still reachable via Home button + tab click. admin-bar.js DS 0.8.13 → 0.8.14. 0 dashboard HTML files touched. |
| 2026-05-27 | build | forge/index.html | 0.1.0 initial intro page. Basic placeholder for the new Forge agent (Practice Steward, sibling of Broker). NOT a data-driven dashboard yet — hardcoded intro content + workflow explanation. Built from `_design/_template/` (Sprint 5 workflow). Forge-specific tokens (`--forge` #9B4F3A warm brick red + dark variant) locally in `:root`. Panels: mental-model quote block, "How to use me" 4-card grid (when-to-call / capture / refine / handoff with command examples), 2 active practice area cards (Inference Farm + Microsites with go-links to NOTES), 9-mode v0.2 preview list, callout for placeholder state. Footer: agent canonical + 2 practice indexes + sibling Broker dashboard. Card copy-ref (DS §4a) on every card-like element. Shared engine: tokens.css + theme.js + clipboard.js + dom-utils.js + admin-bar.js (NO live-updates / markdown-parser — basic intro page, no data fetch yet). Lint: 100% green (regular + --strict). dashboard_count 20 → 21. Full Forge observatory dashboard (kanban + summary + recent inbox-flow + learning lifecycle UI) v0.2. |
| 2026-05-28 | tend | alfred/index.html + todos store | 0.1.0 → 0.2.0. „Feladatok scope szerint" panel (panel 02). Markdown-natív TODO-rendszer: `02_Areas/Personal Growth/Alfred/todos/<scope>.md` checkbox-listák (00_TODOS.md konvenció + personal.md + family.md, példa Task A/B/C). Parse: `## Active` checkboxok → státusz (open/done) + due (📅 / due::) + prioritás (⏫/🔼/🔽) + scope (#tag). Per-scope grouping nyitott+archív számmal; due-state chip (overdue/today/soon/none, élő óra). `TODO_SCOPES` config (5 scope, hiányzó fájl graceful skip) + `parseTasks`/`dueState` helper. Panelek átszámozva (sync-ritual → 06, modes → 07). Due/prio token-reuse (--gap/--accent/--alfred, nincs új hex). Lint: 100% zöld. NINCS adatbázis — markdown a forrás (Alfred §5a döntés). |
| 2026-05-28 | build | alfred/index.html | 0.1.0 initial build. First Alfred dashboard (Executive Cognition Layer — the BDOS human interface layer). Built from `_design/_template/` (Sprint 5 workflow). 8 panels: status strip (4 stat from last_run.md frontmatter: last_sync/last_briefing/inbox_backlog/pending_routes), sync-ritual architecture (3-layer flow: raw capture → processing → structured operational), cognitive inbox (live from `inbox.md` Unprocessed section bullets, „inbox clear" empty-state), today briefing (last_run.md body „Mai briefing" section, never_run empty-state), priorities (3 buckets parsed from `priorities.md`), modes (4 v0.1 mag + 5 v0.2 tbd), about footer (collapsible). Body section parser (`getSection` + `bulletItems` + `isPlaceholder` helpers, no write-back). Alfred tokens (`--alfred`/`-deep`/`-tint`, slate-blue butler) locally in `:root` light + dark. All shared engine + SSE live-updates. Card copy-ref (DS §4a) on all stat/card/lineitem. Graceful never_run empty-state on every data panel. Storage home bootstrapped: `02_Areas/Personal Growth/Alfred/` (NOTES + inbox + state/last_run + priorities). dashboard_count 21 → 22. |
| 2026-05-28 | promote (DS 0.8.14 → 0.9.0, family-wide, all 22 dashboards) | all 22 HTML files | Alfred Tasks Sidebar rollout. DS §12 "Alfred Tasks Sidebar" codified. Shared helper `_design/alfred-tasks.js` created (parseTasks/dueState/render/injectStyles, 30s poll, CustomEvent API). Two-column shell: `.app-shell` (flex row, max-width 1440px) + `.app-main` (flex:1, per-dashboard max-width preserved) + `#alfred-tasks-sidebar` (240px, sticky, responsive stacks below 900px). CSS injected by alfred-tasks.js — NOT in per-dashboard `<style>` blocks. Drawers/scrims/modals remain outside `.app-shell` on all affected files. Version bumps: index 0.8.11→0.8.12, sales 0.7.9→0.8.0, account-management 0.1.1→0.1.2, partnerships 0.2.8→0.2.9, navigator 0.2.0→0.2.1, aiops 0.1.9→0.2.0, team 0.2.0→0.2.1, plugins 0.1.8→0.1.9, agents 0.2.5→0.2.6, sage 0.5.7→0.5.8, maestro 0.5.8→0.5.9, presto 0.18.4→0.18.5, librarian 0.6.0→0.6.1, curator 0.5.8→0.5.9, scheduler 0.1.7→0.1.8, personal-growth 0.4.0→0.4.1, presences 0.2.1→0.2.2, broker 0.5.8→0.5.9, forge 0.2.0→0.2.1, alfred 0.2.0→0.2.1, vezetok-imaja 0.1.0→0.1.1. |
| 2026-05-28 | tend (central-modal registration) | index.html + admin-bar.js | Alfred registered in the central modal layer. admin-bar.js: AGENT_CARDS gained 8th entry (Alfred 🤵, `/_dashboards/alfred/index.html`) — appears in the Agent Quick-Nav popup on every dashboard + letter-jump (A-key collision: Alfred shares 'A'-row; first-wins). Launcher index.html: AGENT_DASHBOARD_URLS gained `alfred` (+ `forge` backfilled — its live dashboard was unregistered) so the Agents-tab graph node opens the live dashboard instead of coming-soon. STATIC_EDGES: 4 Alfred permitted-flow edges added (alfred→sage/presto/broker/forge, label „route→") reflecting Alfred-as-dispatcher. Alfred graph node auto-appears from `00_AGENTS_INDEX.md` parse (### Alfred — v0.1.0). |
| 2026-05-27 | tend (major rewrite) | forge/index.html + 2 practice NOTES | 0.1.0 → 0.2.0. Hardcoded intro page → data-driven practice observatory. Per-practice NOTES frontmatter extended with at-a-glance fields: `strategic_directions` (array, 1-3 bullets), `next_step` (string), `deadline` (date or null), `blocker` (string or null), `top_todos` (array, max 3 shown), `counts` block (open_questions / related_engagements / learnings_active / learnings_proposed), `unit_display` + `unit_short` (for dashboard grouping). Inference Farm + Microsites NOTES updated with these fields. Dashboard now: (1) fetches PRACTICE_NOTES_PATHS (hardcoded 2-entry list, future v0.3 discovers from per-unit 00_INDEX.md), (2) parses YAML frontmatter via shared markdown-parser.js, (3) groups by unit_display, (4) renders launcher-style unit containers (CPS, ExarLabs) with practice cards inside. Practice cards: stage badge (6 semantic stages with color tokens), owner italic line, strategic-direction bullets, next-step highlight block, deadline chip (overdue/today/soon/planned/none — day-count computed), blocker chip (red, conditional), top-TODO checklist (max 3), counts row, Open NOTES link. New token set: 6 stage colors + 4 deadline-state colors + 1 forge identity (light + dark variants). live-updates.js wired (SSE 8s sync — when user edits NOTES in Obsidian, dashboard refreshes). Howto + modes moved to collapsible `<details class="about">` footer. Lint: 100% green (regular + --strict). |
| 2026-05-28 | tend (wide-screen kanban) | account-management.html + sales.html | Both kanban dashboards now use the full viewport width on wide monitors (user request). Above the `.app` 1480px cap, the kanban panel breaks out full-bleed (`margin: calc(50% - 50vw); width: 100vw; 36px gutters; side borders/radius dropped`) via `@media (min-width:1481px)`, while text panels (masthead/today/summary/filters) stay centered & readable. AM (`#panel-2-kanban`): all 10 stage columns spread across the width. Sales (`#board.is-kanban`): `repeat(auto-fit, minmax(280px,1fr))` so as many pipeline stages as fit spread per row instead of wrapping at 1480px. Pure CSS, no token/JS change. account-management.html 0.1.0 → 0.1.1; sales.html 0.7.8 → 0.7.9. Lint: AM 100% green; sales clean except 2 pre-existing warnings (31 legacy custom hex + 1 direct EventSource — not introduced by this change). |
