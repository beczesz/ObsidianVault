# (Monday)

## 🗂 Projects & Tasks

``` dataview
TASK
FROM "01_Projects"
WHERE !completed AND due <= date(today) and due
GROUP BY file.folder + "/" + file.name
```

## 📝Areas TODO

``` dataview
TASK FROM "02_Areas" WHERE !completed AND due <= date(today) and due GROUP BY link(file.path)
```

### 📝 General tasks

- [ ] 

  
## [[Personal Areas]]

```habittracker
{
	"path": "02_Areas/Personal Growth/Habits/"
}
```

Weight:: 95.5
```dataviewjs
// Get the date one week ago
const target = dv.date("today").minus({ days: 7 });
const targetName = target.toFormat("yyyy-MM-dd");

// Search all daily notes and pick the one whose filename matches the date
const page = dv.pages('"05_DailyNotes"').where(p => p.file.name === targetName).first();

if (!page) {
  dv.paragraph(`No daily note found for: **${targetName}**`);
} else {
  // Dataview will usually read inline fields like Weight:: ...
  let w = page.Weight;

  // If you use decimal comma, normalize to a number for safety
  if (typeof w === "string") w = Number(w.replace(",", "."));

  dv.paragraph(`Weight: 1 week ago (**${targetName}**) was **${w ?? "missing"}** kg`);
}
```


```button 
name Refresh Graph
type command
action Reload app without saving
```
```tracker
searchType: dvField
searchTarget: Weight
folder: 05_DailyNotes
dateFormat: 'YYYY-MM-DD'   # match your daily note filenames
startDate: -20d
endDate: 0d

line:
  title: Weight (kg)
  yAxisLabel: kg
  showPoint: true
  yMin: 90
```

